# W7 — 24h soak. Loop: POST /v1/clip (bearer) + GET /v1/clip/latest, assert 200s, sample
# clipbeam-tray RSS via Get-Process every 5 min into soak.csv. Fail on any 5xx or RSS > 30MB.
Write-Host "TODO W7 — see docs/PHASE1_TASKS.md"
